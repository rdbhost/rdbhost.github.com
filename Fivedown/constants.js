const pi = Math.PI;
const e = Math.E;

dedpendents = []
function traverse() {
    var = 4
    foreach(v =>{
        v.includes(var ? var = var + newvar and traverse() : skip)
    })
}

traverseLVL2{
    var = 16
    foreach(v =>{
         v.includes(var ? var = var + newvar and traverse() : skip)
    })
}

0: (4) ["Height", "h", "", "4"]
1: (4) ["Volume of a Cube", "vol_cube", "area_square * 4", "8"]
2: (4) ["Area of a square", "area_square", "h^2", "4"]
3: (4) ["", "", "", ""]
